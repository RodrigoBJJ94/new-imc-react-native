import { StyleSheet } from "react-native";

export default StyleSheet.create({
    titleView: {
        backgroundColor: "#9e9e9e",
        padding: 20,
    },
    title: {
        textAlign: "center",
        fontSize: 26,
        fontWeight: "bold",
        color: "#fff",
    }
});
